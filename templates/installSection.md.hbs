{{> preinstall}}
## Installation

Install `{{pkg.name}}`{{#if pkg.preferGlobal }} globally{{/if}}{{#if pkg.peerDependencies}} and it's peers: {{#each pkg.peerDependencies}}{{#unless @first}}, {{/unless}}`{{@key}}`{{/each}}{{/if}} using [npm](https://www.npmjs.com/):

```bash
npm install {{#if pkg.preferGlobal }}-g{{else}}--save{{/if}}{{#each pkg.peerDependencies}} {{@key}}{{/each}} {{pkg.name}}
```
{{> postinstall}}
